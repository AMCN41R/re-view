{
  "name": "re-view",
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "npm install && lerna bootstrap",
    "build": "lerna run build --stream",
    "build:client": "lerna run build --scope review-client --stream",
    "build:server": "lerna run build --scope review-server --stream",
    "build:run:server": "lerna run build:run --scope review-server --stream",
    "start": "lerna run start --scope review-server --stream"
  },
  "dependencies": {
    "lerna": "3.13.1"
  },
  "engines": {
    "node": "10.15.2"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.28.0",
    "@typescript-eslint/parser": "^2.28.0",
    "eslint": "^6.8.0"
  }
}
